import {BrowserRouter, Route, RouterProvider, Routes} from "react-router-dom"

import Router from "./Router";

function App(){
  return(
    <div>
      <Router />
    </div>
  )
}

export default App;